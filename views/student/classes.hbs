<ul class="list-unstyled">
    {{#each classes}}
    <li>
        <div class="class-block rounded" onclick="window.location='/course?name={{this.name}}'">
            <h3>{{this.name}}</h3>
            <p>{{this.professor}}</p>
        </div>
    </li>
    {{/each}}
</ul>